# dependencies (SECURITY_AUDIT.md L1 — node_modules was committed: 1,980 files)
node_modules/

# secrets — never commit real keys
.env
.env.*
!.env.example

# platform / build
.vercel/
.netlify/
dist/
build/

# os / editor noise
.DS_Store
Thumbs.db
*.log
